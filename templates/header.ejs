<!DOCTYPE html>
<html>

	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">

		<link rel="icon" type="image/png" sizes="32x32" href="/assets/favicon.png" />
		<link rel="alternate" type="application/atom+xml" href="feed" />

		<title><%= title %> / <%= config.siteTitle %></title>

		<style>
			/*

				╔═════════╗
				║ C S S   ║
				║ I S     ║
				║ A W E S O M E
				╚═════════╝

				regrets, i have a few
			*/

			:root {
				--color-bg-rgb: 255, 255, 255;
				--color-code-rgb: 245, 235, 230;
				--color-link-rgb: 214, 93, 14;
				--color-text-rgb: 40, 40, 40;

				--color-bg: rgb( var( --color-bg-rgb ) );
				--color-code: rgb( var( --color-code-rgb ) );
				--color-link: rgb( var( --color-link-rgb ) );
				--color-link-muted: rgba( var( --color-link-rgb ), 0.5 );
				--color-nav: rgba( var( --color-text-rgb ), 0.6 );
				--color-text: rgb( var( --color-text-rgb ) );
			}

			@media (prefers-color-scheme: dark) {
				:root {
					--color-bg-rgb: 40, 40, 40;
					--color-code-rgb: 80, 73, 68;
					--color-link-rgb: 254, 128, 25;
					--color-text-rgb: 221, 221, 221;
				}
			}

			html {
				background-color: var( --color-bg );
				color: var( --color-text );
				line-height: 1.4;
				margin: 0 1em;
			}

			body {
				font-family: -apple-system, system-ui, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, Arial, sans-serif;
				font-size: 1.2em;
				margin: 1em auto;
				max-width: 45rem;
			}

			nav,
			footer {
				color: var( --color-nav );
				font-size: 0.9em;
			}

			h1,
			h2,
			h3,
			h4,
			h5,
			h6 {
				line-height: 1.25;
			}

			a {
				color: var( --color-link );
				text-decoration-color: var( --color-link-muted );
			}

			main,
			.archive {
				margin: 4em 0;
			}

			article img {
				height: auto;
				position: relative;
				width: 100%;
			}

			code {
				background-color: var( --color-code );
				font-size: 1.05em;
				padding: 3px 5px;
			}

			pre code {
				display: block;
				overflow-x: auto;
				padding: 0.25em;
			}

			nav ul {
				list-style: none;
				margin-bottom: 2.5em;
				padding: 0;
			}

			li {
				margin: 0.25em 0;
			}

			footer h3 {
				color: var( --color-text );
			}
		</style>
	</head>

	<body>

		<header>
			<nav>
				<h2 class="title"><%= config.siteTitle.toLowerCase() %></h2>
			</nav>
		</header>

		<main>
